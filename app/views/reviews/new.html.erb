<%= form_for [@meme, @review] do |f| %>
  <%= f.label(:rating), "Rating:" %>
  <%= f.text_field(:rating) %>
  <%= f.label(:body), "Comments:" %>
  <%= f.text_field(:body) %>

  <%= f.submit("Submit Review") %>
<% end %>
